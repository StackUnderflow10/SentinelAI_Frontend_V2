export default function Footer() {
  return (
    <footer className="text-center px-6 py-5 border-t border-line font-mono text-[11px] text-inkDim">
      Sentinel AI — automated analysis is a signal, not a verdict. Verify with
      primary sources.
    </footer>
  );
}